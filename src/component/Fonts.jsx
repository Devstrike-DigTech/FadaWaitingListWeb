// src/components/FontLoader.js
import { createGlobalStyle } from 'styled-components';

const FontLoader = createGlobalStyle`
  @import url('https://fonts.googleapis.com/css2?family=Alexandar&display=swap');

  body {
    font-family: 'Alexandar', sans-serif;
  }
`;

export default FontLoader;
